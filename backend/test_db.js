const mongoose = require('mongoose'); const User = require('./models/User'); const Institution = require('./models/Institution'); const Task = require('./models/Task'); mongoose.connect('mongodb://127.0.0.1:27017/talent-collab').then(async () => { console.log('USERS:', await User.find({}, 'email role institutionId')); console.log('INSTS:', await Institution.find({}, '_id name')); console.log('TASKS:', await Task.find({}, 'type targetEmail senderEmail status cvId targetVacancyId').sort({createdAt: -1}).limit(10)); process.exit(0); });
